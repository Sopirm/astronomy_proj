<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Космический Дашборд | Кассиопея' %></title>
  
  <!-- CSS Libraries -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  
  <!-- Custom Theme -->
  <link rel="stylesheet" href="/css/space-theme.css">
  
  <!-- Advanced Custom CSS -->
  <style>
    /* Advanced Space Theme */
    :root {
      /* Color Palette */
      --space-dark: #030712;
      --space-primary: #0f172a;
      --space-secondary: #1e293b;
      --space-tertiary: #334155;
      
      /* Accent Colors */
      --neon-blue: #00d9ff;
      --neon-purple: #b084ff;
      --neon-green: #00ff88;
      --neon-pink: #ff0080;
      --neon-yellow: #ffff00;
      
      /* Text Colors */
      --text-primary: #f8fafc;
      --text-secondary: #cbd5e1;
      --text-muted: #64748b;
      
      /* Gradients */
      --gradient-cosmic: linear-gradient(135deg, #030712 0%, #1e1b4b 50%, #312e81 100%);
      --gradient-aurora: linear-gradient(45deg, #00d9ff, #b084ff, #00ff88);
      --gradient-nebula: radial-gradient(circle at 30% 70%, #ff008040 0%, transparent 50%), 
                         radial-gradient(circle at 70% 20%, #00d9ff40 0%, transparent 50%), 
                         radial-gradient(circle at 50% 50%, #b084ff20 0%, transparent 50%);
    }

    /* Global Styles */
    * {
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }
    
    body { 
      background: var(--space-dark);
      background-image: 
        var(--gradient-nebula),
        linear-gradient(135deg, var(--space-dark) 0%, var(--space-primary) 50%, var(--space-secondary) 100%);
      background-attachment: fixed;
      color: var(--text-primary);
      min-height: 100vh;
      margin: 0;
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      overflow-x: hidden;
    }

    /* Animated Background Stars */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: 
        radial-gradient(2px 2px at 20px 30px, #fff, transparent),
        radial-gradient(2px 2px at 40px 70px, rgba(255,255,255,0.8), transparent),
        radial-gradient(1px 1px at 90px 40px, #fff, transparent),
        radial-gradient(1px 1px at 130px 80px, rgba(255,255,255,0.6), transparent),
        radial-gradient(2px 2px at 160px 30px, #fff, transparent);
      background-repeat: repeat;
      background-size: 200px 100px;
      animation: twinkle 10s infinite linear;
      z-index: -1;
      opacity: 0.3;
    }

    @keyframes twinkle {
      0% { transform: translateY(0px); }
      100% { transform: translateY(-100px); }
    }

    /* Advanced Cards */
    .space-card {
      background: rgba(15, 23, 42, 0.7);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(0, 217, 255, 0.2);
      border-radius: 16px;
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
    }

    .space-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(0, 217, 255, 0.1), transparent);
      transition: left 0.8s ease;
    }

    .space-card:hover {
      transform: translateY(-8px) scale(1.02);
      box-shadow: 
        0 20px 40px rgba(0, 217, 255, 0.3),
        0 0 20px rgba(176, 132, 255, 0.2),
        inset 0 1px 0 rgba(255, 255, 255, 0.1);
      border-color: rgba(0, 217, 255, 0.5);
    }

    .space-card:hover::before {
      left: 100%;
    }

    /* Glow Effects */
    .glow-blue {
      box-shadow: 0 0 20px rgba(0, 217, 255, 0.5);
      animation: glow-pulse-blue 2s infinite alternate;
    }

    .glow-purple {
      box-shadow: 0 0 20px rgba(176, 132, 255, 0.5);
      animation: glow-pulse-purple 2s infinite alternate;
    }

    .glow-green {
      box-shadow: 0 0 20px rgba(0, 255, 136, 0.5);
      animation: glow-pulse-green 2s infinite alternate;
    }

    @keyframes glow-pulse-blue {
      0% { box-shadow: 0 0 20px rgba(0, 217, 255, 0.3); }
      100% { box-shadow: 0 0 30px rgba(0, 217, 255, 0.7); }
    }

    @keyframes glow-pulse-purple {
      0% { box-shadow: 0 0 20px rgba(176, 132, 255, 0.3); }
      100% { box-shadow: 0 0 30px rgba(176, 132, 255, 0.7); }
    }

    @keyframes glow-pulse-green {
      0% { box-shadow: 0 0 20px rgba(0, 255, 136, 0.3); }
      100% { box-shadow: 0 0 30px rgba(0, 255, 136, 0.7); }
    }

    /* Loading Animations */
    .loading {
      animation: cosmic-pulse 1.5s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }

    @keyframes cosmic-pulse {
      0%, 100% { 
        opacity: 1;
        transform: scale(1);
      }
      50% { 
        opacity: 0.7;
        transform: scale(1.1);
      }
    }

    /* Floating Animation */
    .float {
      animation: float 3s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
    }

    /* Text Glow */
    .text-glow {
      text-shadow: 0 0 10px currentColor;
    }

    /* Button Styles */
    .btn-cosmic {
      background: linear-gradient(45deg, var(--neon-blue), var(--neon-purple));
      border: none;
      color: white;
      padding: 12px 24px;
      border-radius: 30px;
      font-weight: 600;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .btn-cosmic:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(0, 217, 255, 0.4);
    }

    .btn-cosmic::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
      transition: left 0.5s ease;
    }

    .btn-cosmic:hover::before {
      left: 100%;
    }

    /* Maps */
    #map { 
      height: 340px; 
      border-radius: 16px;
      border: 2px solid rgba(0, 217, 255, 0.3);
    }

    /* Scrollbar */
    ::-webkit-scrollbar {
      width: 8px;
    }

    ::-webkit-scrollbar-track {
      background: rgba(15, 23, 42, 0.5);
      border-radius: 4px;
    }

    ::-webkit-scrollbar-thumb {
      background: linear-gradient(180deg, var(--neon-blue), var(--neon-purple));
      border-radius: 4px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: linear-gradient(180deg, var(--neon-purple), var(--neon-blue));
    }

    /* Responsive adjustments */
    @media (max-width: 768px) {
      .space-card:hover {
        transform: none;
      }
    }
  </style>
</head>
<body>
  <!-- Advanced Navigation -->
  <nav class="fixed top-0 left-0 right-0 z-50 bg-slate-900 bg-opacity-80 backdrop-filter backdrop-blur-xl border-b border-neon-blue border-opacity-30">
    <div class="container mx-auto px-6">
      <div class="flex items-center justify-between h-20">
        <!-- Logo & Brand -->
        <div class="flex items-center space-x-8">
          <a href="/dashboard" class="group flex items-center space-x-3">
            <div class="relative">
              <div class="text-3xl float glow-blue">🚀</div>
              <div class="absolute inset-0 bg-neon-blue rounded-full opacity-20 blur-xl group-hover:opacity-40 transition-opacity"></div>
            </div>
            <div class="text-xl font-bold bg-gradient-to-r from-neon-blue to-neon-purple bg-clip-text text-transparent">
              Кассиопея
            </div>
          </a>
          
          <!-- Main Navigation -->
          <div class="hidden lg:flex items-center space-x-1">
            <a href="/dashboard" class="nav-item <%= (currentPath === '/dashboard') ? 'active' : '' %>" data-tooltip="Общий обзор системы">
              <span class="nav-icon">📊</span>
              <span class="nav-text">Дашборд</span>
            </a>
            <a href="/iss" class="nav-item <%= (currentPath === '/iss') ? 'active' : '' %>" data-tooltip="Трекинг МКС">
              <span class="nav-icon">🛸</span>
              <span class="nav-text">МКС</span>
            </a>
            <a href="/jwst" class="nav-item <%= (currentPath === '/jwst') ? 'active' : '' %>" data-tooltip="Галерея телескопа">
              <span class="nav-icon">🔭</span>
              <span class="nav-text">JWST</span>
            </a>
            <a href="/osdr" class="nav-item <%= (currentPath === '/osdr') ? 'active' : '' %>" data-tooltip="Космические эксперименты">
              <span class="nav-icon">🧪</span>
              <span class="nav-text">OSDR</span>
            </a>
            <a href="/astro" class="nav-item <%= (currentPath === '/astro') ? 'active' : '' %>" data-tooltip="Астрономические события">
              <span class="nav-icon">⭐</span>
              <span class="nav-text">Астро</span>
            </a>
            <a href="/legacy" class="nav-item <%= (currentPath === '/legacy') ? 'active' : '' %>" data-tooltip="Legacy данные">
              <span class="nav-icon">📈</span>
              <span class="nav-text">Legacy</span>
            </a>
          </div>
        </div>
        
        <!-- Right Side -->
        <div class="flex items-center space-x-6">
          <!-- System Status -->
          <div class="hidden md:flex items-center space-x-3 px-4 py-2 bg-slate-800 bg-opacity-50 rounded-full border border-neon-green border-opacity-30">
            <div class="flex items-center space-x-2">
              <div id="health-status" class="w-3 h-3 bg-neon-green rounded-full animate-pulse glow-green"></div>
              <span class="text-sm font-medium text-neon-green">Система онлайн</span>
            </div>
            <div class="w-px h-4 bg-slate-600"></div>
            <div class="text-xs text-slate-400" id="current-time">
              <!-- Time will be updated by JS -->
            </div>
          </div>

          <!-- Settings Button -->
          <button class="p-3 hover:bg-slate-800 hover:bg-opacity-50 rounded-full transition-all duration-300 group">
            <div class="text-xl group-hover:rotate-90 transition-transform duration-300">⚙️</div>
          </button>

          <!-- Mobile Menu Button -->
          <button id="mobile-menu-btn" class="lg:hidden p-2 hover:bg-slate-800 hover:bg-opacity-50 rounded-lg transition-all">
            <div class="w-6 h-6 flex flex-col justify-center items-center space-y-1">
              <div class="w-4 h-0.5 bg-neon-blue transition-all menu-line-1"></div>
              <div class="w-4 h-0.5 bg-neon-blue transition-all menu-line-2"></div>
              <div class="w-4 h-0.5 bg-neon-blue transition-all menu-line-3"></div>
            </div>
          </button>
        </div>
      </div>
    </div>

    <!-- Mobile Navigation -->
    <div id="mobile-nav" class="lg:hidden absolute top-full left-0 right-0 bg-slate-900 bg-opacity-95 backdrop-blur-xl border-b border-neon-blue border-opacity-20 transform -translate-y-full opacity-0 transition-all duration-300">
      <div class="container mx-auto px-6 py-4">
        <div class="space-y-2">
          <a href="/dashboard" class="mobile-nav-item <%= (currentPath === '/dashboard') ? 'active' : '' %>">
            <span class="nav-icon">📊</span>
            <span>Дашборд</span>
          </a>
          <a href="/iss" class="mobile-nav-item <%= (currentPath === '/iss') ? 'active' : '' %>">
            <span class="nav-icon">🛸</span>
            <span>МКС</span>
          </a>
          <a href="/jwst" class="mobile-nav-item <%= (currentPath === '/jwst') ? 'active' : '' %>">
            <span class="nav-icon">🔭</span>
            <span>JWST</span>
          </a>
          <a href="/osdr" class="mobile-nav-item <%= (currentPath === '/osdr') ? 'active' : '' %>">
            <span class="nav-icon">🧪</span>
            <span>OSDR</span>
          </a>
          <a href="/astro" class="mobile-nav-item <%= (currentPath === '/astro') ? 'active' : '' %>">
            <span class="nav-icon">⭐</span>
            <span>Астро</span>
          </a>
          <a href="/legacy" class="mobile-nav-item <%= (currentPath === '/legacy') ? 'active' : '' %>">
            <span class="nav-icon">📈</span>
            <span>Legacy</span>
          </a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Navigation Spacer -->
  <div class="h-20"></div>

  <!-- Main Content -->
  <main class="container mx-auto px-4 py-6">
    <%- body %>
  </main>

  <!-- Scripts -->
  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" defer></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js" defer></script>
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js" defer></script>
  
  <!-- Space App Core -->
  <script src="/js/space-app-utilities.js" defer></script>

  <!-- Custom Effects -->
  <script src="/js/space-effects.js" defer></script>
</body>
</html>
