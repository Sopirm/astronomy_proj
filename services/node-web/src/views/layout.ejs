<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Космический Дашборд | Кассиопея' %></title>
  
  <!-- CSS Libraries -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  
  <!-- Custom Theme -->
  <link rel="stylesheet" href="/css/space-theme.css">
  
</head>
<body>
  <!-- Advanced Navigation -->
  <nav class="fixed top-0 left-0 right-0 z-50 bg-white border-b border-gray-200">
    <div class="container mx-auto px-6">
      <div class="flex items-center justify-between h-16">
        <!-- Logo & Brand -->
        <div class="flex items-center space-x-3">
          <a href="/dashboard" class="group flex items-center space-x-2">
            <div class="relative">
              <div class="text-2xl">🚀</div>
            </div>
            <div class="text-xl font-bold text-gray-900">
              Кассиопея
            </div>
          </a>
          
          <!-- Main Navigation -->
          <div class="hidden lg:flex items-center space-x-1">
            <a href="/dashboard" class="nav-item <%= (currentPath === '/dashboard') ? 'active' : '' %>" data-tooltip="Общий обзор системы">
              <span class="nav-icon">📊</span>
              <span class="nav-text">Дашборд</span>
            </a>
            <a href="/jwst" class="nav-item <%= (currentPath === '/jwst') ? 'active' : '' %>" data-tooltip="Галерея телескопа">
              <span class="nav-icon">🔭</span>
              <span class="nav-text">JWST</span>
            </a>
            <a href="/osdr" class="nav-item <%= (currentPath === '/osdr') ? 'active' : '' %>" data-tooltip="Космические эксперименты">
              <span class="nav-icon">🧪</span>
              <span class="nav-text">OSDR</span>
            </a>
            <a href="/astro" class="nav-item <%= (currentPath === '/astro') ? 'active' : '' %>" data-tooltip="Астрономические события">
              <span class="nav-icon">⭐</span>
              <span class="nav-text">Астро</span>
            </a>
            <a href="/legacy" class="nav-item <%= (currentPath === '/legacy') ? 'active' : '' %>" data-tooltip="Legacy данные">
              <span class="nav-icon">📈</span>
              <span class="nav-text">Legacy</span>
            </a>
          </div>
        </div>
        
        <!-- Right Side -->
        <div class="flex items-center space-x-4">
          <!-- System Status -->
          <div class="hidden md:flex items-center space-x-2 px-3 py-1 bg-gray-100 rounded-full border border-gray-200">
            <div class="flex items-center space-x-1">
              <div id="health-status" class="w-2.5 h-2.5 bg-green-500 rounded-full"></div>
              <span class="text-xs font-medium text-gray-700">Система онлайн</span>
            </div>
            <div class="w-px h-3 bg-gray-300"></div>
            <div class="text-xs text-gray-500" id="current-time">
              <!-- Time will be updated by JS -->
            </div>
          </div>

          <!-- Settings Button -->
          <button class="p-2 hover:bg-gray-100 rounded-full transition-colors duration-200 group">
            <div class="text-lg group-hover:rotate-45 transition-transform duration-200">⚙️</div>
          </button>

          <!-- Mobile Menu Button -->
          <button id="mobile-menu-btn" class="lg:hidden p-2 hover:bg-gray-100 rounded-lg transition-colors">
            <div class="w-6 h-6 flex flex-col justify-center items-center space-y-1">
              <div class="w-4 h-0.5 bg-blue-500 transition-all menu-line-1"></div>
              <div class="w-4 h-0.5 bg-blue-500 transition-all menu-line-2"></div>
              <div class="w-4 h-0.5 bg-blue-500 transition-all menu-line-3"></div>
            </div>
          </button>
        </div>
      </div>
    </div>

    <!-- Mobile Navigation -->
    <div id="mobile-nav" class="lg:hidden absolute top-full left-0 right-0 bg-white border-b border-gray-200 transform -translate-y-full opacity-0 transition-all duration-200">
      <div class="container mx-auto px-6 py-3">
        <div class="space-y-1">
          <a href="/dashboard" class="mobile-nav-item <%= (currentPath === '/dashboard') ? 'active' : '' %>">
            <span class="nav-icon">📊</span>
            <span>Дашборд</span>
          </a>
          <a href="/jwst" class="mobile-nav-item <%= (currentPath === '/jwst') ? 'active' : '' %>">
            <span class="nav-icon">🔭</span>
            <span>JWST</span>
          </a>
          <a href="/osdr" class="mobile-nav-item <%= (currentPath === '/osdr') ? 'active' : '' %>">
            <span class="nav-icon">🧪</span>
            <span>OSDR</span>
          </a>
          <a href="/astro" class="mobile-nav-item <%= (currentPath === '/astro') ? 'active' : '' %>">
            <span class="nav-icon">⭐</span>
            <span>Астро</span>
          </a>
          <a href="/legacy" class="mobile-nav-item <%= (currentPath === '/legacy') ? 'active' : '' %>">
            <span class="nav-icon">📈</span>
            <span>Legacy</span>
          </a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Navigation Spacer -->
  <div class="h-16"></div>

  <!-- Main Content -->
  <main class="container mx-auto px-4 py-6">
    <%- body %>
  </main>

  <!-- Scripts -->
  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" defer></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js" defer></script>
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js" defer></script>
  
  <!-- Space App Core -->
  <script src="/js/space-app-utilities.js" defer></script>

  <!-- Custom Effects -->
  <script src="/js/space-effects.js" defer></script>
</body>
</html>
