<!-- Error Page -->
<div class="min-h-screen flex items-center justify-center animate__animated animate__fadeIn">
  <div class="text-center space-card rounded-lg p-8 max-w-md w-full mx-4">
    <!-- Error Icon -->
    <div class="text-6xl text-red-400 mb-4">
      <% if (status === 404) { %>
        🌌
      <% } else { %>
        ⚠️
      <% } %>
    </div>
    
    <!-- Error Status -->
    <h1 class="text-4xl font-bold text-red-400 mb-2">
      <%= status || 500 %>
    </h1>
    
    <!-- Error Title -->
    <h2 class="text-xl font-semibold text-gray-300 mb-4">
      <% if (status === 404) { %>
        Страница затерялась в космосе
      <% } else { %>
        Системная ошибка
      <% } %>
    </h2>
    
    <!-- Error Message -->
    <p class="text-gray-400 mb-6">
      <%= error || 'Произошла неожиданная ошибка' %>
    </p>
    
    <!-- Action Buttons -->
    <div class="space-y-3">
      <button onclick="history.back()" 
              class="w-full px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-lg transition-colors">
        ← Назад
      </button>
      
      <a href="/dashboard" 
         class="block w-full px-6 py-3 bg-gray-600 hover:bg-gray-700 text-white rounded-lg transition-colors">
        🏠 На главную
      </a>
    </div>
    
    <!-- Help Links -->
    <div class="mt-6 pt-4 border-t border-gray-600">
      <p class="text-sm text-gray-500 mb-2">Нужна помощь?</p>
      <div class="flex justify-center space-x-4 text-sm">
        <a href="/dashboard" class="text-blue-400 hover:text-blue-300">Дашборд</a>
        <a href="/health" class="text-green-400 hover:text-green-300">Статус</a>
      </div>
    </div>
  </div>
</div>

<script>
// Автоматическое перенаправление через 10 секунд для 5xx ошибок
<% if (status >= 500) { %>
setTimeout(() => {
  window.location.href = '/dashboard';
}, 10000);
<% } %>
</script>
